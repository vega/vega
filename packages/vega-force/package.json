{
  "name": "vega-force",
  "version": "3.0.0",
  "description": "Force simulation transform for Vega dataflows.",
  "keywords": [
    "vega",
    "force"
  ],
  "license": "BSD-3-Clause",
  "author": "Jeffrey Heer (http://idl.cs.washington.edu)",
  "main": "build/vega-force.js",
  "module": "index",
  "repository": "vega/vega",
  "scripts": {
    "build": "yarn test && terser build/vega-force.js -c -m -o build/vega-force.min.js",
    "pretest": "rm -rf build && mkdir build && rollup -g d3-force:d3,vega-dataflow:vega,vega-util:vega -f umd -n vega.transforms -o build/vega-force.js -- index.js",
    "test": "tape 'test/**/*-test.js' && eslint index.js src test",
    "prepublishOnly": "yarn build",
    "postpublish": "git push && git push --tags"
  },
  "dependencies": {
    "d3-force": "^1.1.2",
    "vega-dataflow": "^4.0.4",
    "vega-util": "^1.7.0"
  },
  "devDependencies": {
    "vega-transforms": "*"
  }
}
